spring:
  freemarker:
    template-loader-path:
     - classpath:/templates/
     - classpath:/
    request-context-attribute: request
  mvc:
    static-path-pattern: /static/**
  profiles:
    active: release
server:
  port: 10001
